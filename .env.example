# Optional in development because Vite proxies /api and /socket.io to localhost:5000.
# In production, set these to your deployed backend if it is not the default Render URL.
VITE_API_BASE_URL=https://vibesphere-backend-plut.onrender.com/api
VITE_SOCKET_URL=https://vibesphere-backend-plut.onrender.com
